server:
  port: 8080

debug: false

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/bd_citas?useUnicode=true&characterEncoding=utf8&useSSL=false&requireSSL=false&AllowPublicKeyRetrieval=True
    username: root
    password: 
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-5%p} %m%n"